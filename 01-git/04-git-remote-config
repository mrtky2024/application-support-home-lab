## Connect Local Repository to GitHub Repository (Link the local repository to a GitHub repository)
git remote add origin "GitHub_Repo_SSH_URL"

##Verify Remote Repository (Check the configured remote repository)
git remote -v
