# Git Commit (Create a snapshot of staged changes)

## 1. Create a Commit (Create a snapshot of all files in the staging area)
git commit -m "Commit message"
---
## 2. Verify Commit History (Check commit history)
git log

